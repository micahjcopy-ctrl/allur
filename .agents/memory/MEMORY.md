- [Owner-only admin gate](admin-owner-gate.md) — gate admin features server-side by comparing OIDC `req.user.id` to `REPL_OWNER_ID`, never on the client.
- [FitCoach static images](fitcoach-static-images.md) — no image-import type decl; put bundled images in `public/` and reference via `import.meta.env.BASE_URL`.
- [AI Coach feature](coach-ai-feature.md) — per-IP limiting needs `trust proxy`; STT detects format by bytes; TTS uses gpt-audio chat (not /audio/speech); only flag planUpdated with non-empty plan.
- [Meal analysis grounding](meal-analysis-grounding.md) — macros grounded in internal `lib/nutrition` DB (no external API); vision only IDs foods + grams; edit foods.ts to expand.
- [FitCoach per-user persistence](fitcoach-persistence.md) — auto-saving a shared client context per-user needs a userId-keyed hydration gate (write-before-read clobber, account-switch leak, admin-seed clobber).
- [ALLUR color system](fitcoach-color-system.md) — "TITANIUM" theme: primary=light titanium-silver (dark fg), info/brand=cyan, success=emerald, warning=amber; edit index.css to re-theme.
- [Landing scoped design system](landing-scoped-design-system.md) — marketing landing is its own brand world (navy + cyan/teal, Space Grotesk sentence-case) under `.allur-lp`; edit `--lp-*`/`.lp-*` in index.css, NOT global `.dark`.
- [Credential auth + OIDC coexistence](credential-oidc-coexistence.md) — sharing one users table: link OIDC by email before insert, keep username/email namespaces disjoint, omit access_token on credential sessions.
- [Volume model dual source](volume-model-dual-source.md) — weekly-set volume numbers are re-encoded client (trainingKnowledge.ts) AND server (fitnessKnowledge.ts); edit both in lockstep.
- [Onboarding dropdown overwrite](onboarding-dropdown-overwrite.md) — rapid Gender/Experience/Activity selection silently reverts a field (stale-closure setProfile) leaving Next disabled with no feedback; pace selections or use functional updates.
- [Exercise demo guide](fitcoach-exercise-guide.md) — Plan tab exercise demos: resolve names via `getExerciseGuide()` (normalized), images in `public/exercises`, restart workflow after adding public/ assets.
- [Exercise optimizer (equipment + prefs)](fitcoach-exercise-optimizer.md) — training-setup constraints; cardio must be name-matched (not meta) to avoid disliked-modality leaks; dislike-filter all alternatives; mirror to coachPrompt by hand.
- [Target physique (gender-aware)](fitcoach-target-physique.md) — stable id vs physiqueLabel(); send the LABEL to every AI call (5 sites incl. Progress×2); gender-split options; normalizePhysique migrates legacy ids.
- [Gmail connector send](gmail-connector-send.md) — transactional email goes through Replit google-mail connector (proxy returns raw Response; Gmail send needs base64url RFC822 in `{raw}`); chosen over Resend for zero domain setup.
- [Screenshot below a 100vh hero](screenshot-below-vh-hero.md) — app-preview captures only top viewport; hash anchors don't scroll on fresh load; temporarily shrink hero min-h / `hidden` upper sections to review below-fold, then revert.
- [ALLUR monetization tiers](allur-monetization-tiers.md) — Free/Base/Premium: derive access from /me/subscription (not credits); server creditGuard is the real enforcement; BASE_MONTHLY_CREDITS dual-sourced.
- [video-js scaffold typecheck](video-js-scaffold.md) — scaffold tsconfig omits DOM lib; tsc errors (window/document/audio) are baseline, verify via Vite + validate-recording.sh, not tsc.
- [Landing live app-screens](landing-live-app-screens.md) — landing "product UI" is hand-built `.allur-lp` screen components (public/ images are AI renders); sticky scrollytelling via useScroll→activeStep; reduced motion via MotionConfig.
- [Single-use token atomicity](single-use-token-atomicity.md) — consume reset/verify tokens with ONE conditional UPDATE...RETURNING (not select-then-update) or concurrent requests replay them.
- [Bodyweight chart entry 1](fitcoach-bodyweight-entries.md) — onboarding weight is a synthetic chart "Start" point, NEVER stored in weightLogs; don't reintroduce any auto-seed effect (caused bogus 1.1 + dup entries).
- [PWA SW API caching](pwa-sw-api-caching.md) — a PWA service worker that caches same-origin GETs must exclude /api/, or it leaks stale/cross-account authenticated data.
- [Stripe go-live in deployments](stripe-replit-sync-prod.md) — 3 silent prod-checkout killers: esbuild drops lib .sql migrations; syncBackfill() no-arg is a no-op (need {object:"all"}); connector returns sandbox unless depl token + environment==="production".
- [PWA standalone vs browser entry](pwa-standalone-entry.md) — installed app shows minimal AppWelcome (not the marketing Landing); `isStandalone()` is the switch in AuthGate; add new signed-out routes to BOTH allow-lists.
- [JSX text unicode escapes](jsx-text-unicode-escapes.md) — \uXXXX is decoded only in JS string literals, NOT in JSX text between tags (renders literally); use the real char or HTML entity there.
- [Landing QR showcase beams](landing-qr-showcase.md) — radial QR beams: use SVG <line> only (preserveAspectRatio=none distorts circles to ellipses); gate install prompt to mobile, desktop routes to /get.
- [Forgot-password diagnosis](forgot-password-diagnosis.md) — "reset never arrived": check password_reset_tokens table first; empty = email never matched (silent 200), not a send failure.
